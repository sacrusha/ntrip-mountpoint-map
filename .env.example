# Real secrets live in .env/cloudflare.conf (the .env directory is gitignored).
# Format below; used by scripts/deploy_pages.ps1.

# Cloudflare API token with Account > Cloudflare Pages > Edit permission.
CF_API_TOKEN=

# Cloudflare account ID (sidebar of dash.cloudflare.com).
CF_ACCOUNT_ID=
