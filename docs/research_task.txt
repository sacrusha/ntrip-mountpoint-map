Research public NTRIP RTK casters for [entry]. Unknown > guess. Output: docs/ntrip_research/ only. File = deliverable; don't pollute caller context until valuable.

NTRIP primer: docs/research_task.primer.txt 

[tools]
WebSearch, WebFetch (incl. PDF URLs), Read, Grep, Glob, Write, Edit, pdftotext.
Bash(curl:*), Bash(nslookup:*), Bash(py scripts/<name>.py:*) ex: Bash(py scripts/stations_by_country.py ZAF)
Different paths, PowerShell, and compound commands will likely be rejected. Try parent domain on failed webfetch, http 0.9 on failed curl.
do NOT author new scripts/*.py to bypass.
other narrow tools (awk, pdftotext, etc.) per-approval when WebFetch result is questionable or conflicts.

[method]
read docs/ntrip_research/[entry], docs/ardusimple/[entry], grep [entry] in docs/networks.md + data/country_markers.json as starting point, don't trust. Use tools to verify, correct, expand, fill gaps. 
Look for national/regional mapping agency in local admin languages, operator portal in primary language, fill gaps with press releases, research papers, RTK hobbyist + professional communities, local survey associations, etc. 
conflicting information -> resolve via multiple sources, date, estimated current reliability of source. WebFetch (Haiku) non-deterministic + prompt-dependent -> retry with directed prompt.
refactor target file, not add. it's a research file, not a research log. There is no requirement to preserve previous style.
no active caster -> most recent project/announcement (date + URL).


[fields] per caster:
- landing_url            operator-owned where available; most-official otherwise
- access_url             best page for user to learn network + signup + conditions
- host:port              curl ST; note date
- tariff                 all tiers; local currency; VAT stated; date observed; source URL
- num_stations           physical CORS count per caster (primer [stations-vs-mps])
- vrs                    yes/no/? -- includes NTRK
- hobbyist_eligibility   yes/no/? (primer [licensing])
- legal_residency_required  yes/no/?
- last_confirmed_alive   date + what tested (sourcetable / login portal)
- datum_epoch            Datum & Epoch + operator-declaration URL. omit if not citable. citation rule + concepts: primer [datum-epoch].

[reachability]
URL unreachable from sandbox acceptable iff: extraordinary evidence target user CAN reach + evidence-backed reason this sandbox cannot.

[ingested-globals]
access rtk2go, centipede, earthscope, euref_ip, igs_ip from local date only, unless those are the networks you are investigating
  py scripts/stations_by_radius.py <lat> <lon> <km>   # use first; reveals country tags per source
  py scripts/stations_by_country.py <code>            # bare = list codes
Don't use for num_stations, sanity check only.
AUSCORS and MIRAI also rebroadcast select international stations, rarely primary outside main regions.  

[changelog]
registration removed; landing_url+access_url added; datum_epoch added; 